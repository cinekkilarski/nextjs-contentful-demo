import client from "./client";
import {
  getEntries,
  getEntry,
  getBlogPosts,
  getBlogPostByTitle,
} from "./queries";

// Export these as named exports directly
export { client, getEntries, getEntry, getBlogPosts, getBlogPostByTitle };
